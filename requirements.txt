# ═══════════════════════════════════════════════════════════
#  Mr.Ham v11 — Streamlit Cloud 배포용 requirements.txt
# ═══════════════════════════════════════════════════════════

# ── 앱 프레임워크 ──
streamlit>=1.40

# ── AI ──
google-genai>=1.0

# ── DB / 인증 ──
supabase>=2.0

# ── 데이터 수집 ──
FinanceDataReader>=0.9.50
yfinance>=0.2.40
requests>=2.31
feedparser>=6.0

# ── 데이터 처리 ──
pandas>=2.0
numpy>=1.24

# ── 이미지 (앱 아이콘 icon.webp 표시용) ──
Pillow>=10.0

# ── 선택: 전종목 수급 데이터 ──
# pykrx 1.2.8+ 는 KRX_ID / KRX_PW 환경변수가 있어야 안정 동작합니다.
# 없어도 앱은 정상 작동하며, 수급 팩터만 중립(50점) 처리됩니다.
# 배포가 느리거나 로그에 "KRX 로그인 실패"가 거슬리면 아래 줄 앞에 #을 붙여 비활성화하세요.
pykrx>=1.0.45
